import {BaseEntity, ObjectLiteral} from "typeorm";
import {CrudInterface} from "./crud.interface";

// export const getContextualEntityService = <T, U extends ObjectLiteral>(entitName: string, entityClass: typeof BaseEntity | any): CrudInterface<T, U> => {
//     // return Container.of().
// }