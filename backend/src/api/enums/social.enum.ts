export enum UserConnectionStatus {
    PENDING = 'PENDING',
    ACCEPTED = 'ACCEPTED',
    DECLINED = 'DECLINED',
    BLOCKED = 'BLOCKED',
}

export enum TrackerVisibility {
    PRIVATE = 'PRIVATE',
    FRIENDS = 'FRIENDS',
    PUBLIC = 'PUBLIC',
}
